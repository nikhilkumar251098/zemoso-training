import Header from "./header";
import { ComponentMeta } from "@storybook/react";

export default {
  title: "Organisms/Header",
  component: Header,
} as ComponentMeta<typeof Header>;

export const Header1 = <Header />;
